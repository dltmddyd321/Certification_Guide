<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cert">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cert">
        <activity android:name=".StartTest"></activity>
        <activity android:name=".Memorization" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DdayActivity" />
        <activity android:name=".TestSchedule" />
        <activity android:name=".TestInfo" />
        <activity android:name=".WhereUsing" />
        <activity android:name=".StudyActivity" />
        <activity
            android:name=".WordTest"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".WrittenTest" />
        <activity android:name=".HowToStudy" />
    </application>

</manifest>